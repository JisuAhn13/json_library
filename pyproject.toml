[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "json-lib"
version = "1.0.0"
description = "JSON 파싱, 직렬화, 파일 I/O, 스키마 검증 라이브러리"
requires-python = ">=3.10"
